model Employee {
  id          String   @id @default(cuid())
  first_name  String
  last_name   String
  modules     Module[]
  customer    Customer @relation(fields: [customer_id], references: [id])
  customer_id String
}
